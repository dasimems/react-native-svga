 perform operations like caching svga before hand on disk as the user can pass in few svga to be cached on disk

When played, it sees if it is available on disk before playing it therefore playing it first on disk if it is available

User can pause svga plays, scale it or style it as they wish, add sounds to play simulateously with it etc
